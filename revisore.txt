smsTOmail's sole core function is cross-device SMS transfer: it detects incoming SMS via RECEIVE_SMS and immediately forwards sender/text by email to the address the user configured, so messages can be read on another device (PC, tablet, phone). No SMS content is stored on remote servers, sold or shared; the email is sent directly from the user's device to their own account. Optional filters only decide which SMS get forwarded.